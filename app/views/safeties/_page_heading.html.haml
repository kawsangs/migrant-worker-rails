= content_for :title do
  = t("category.your_safety")

.d-flex.justify-content-end.mb-2
  = render "shared/buttons/add_new_button", url: new_safety_path, name: t('category.your_safety')

  = render "shared/buttons/more_button" do
    %a.btn.dropdown-item{ href: safeties_path(format: :json), target: '_blank' }
      %i.fas.fa-download.drop-menu-icon
      %span= t('shared.download_json')
