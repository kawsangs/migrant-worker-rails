- errors = f.object.errors[:audio]

.form-group.file-wrapper
  %label= t("category.audio")

  %div{class: ('form-group-invalid' if errors.present?)}
    .form-control.file-control-wrapper{ class: [('d-none' if (errors.blank? && f.object.audio.present?)), ('is-invalid' if errors.present?)] }

      = f.file_field :audio, class: 'file-inputbox', accept: ".mp3,audio/*"

    .invalid-feedback= errors.join('; ')

    - if errors.blank? && f.object.audio.present?
      .remove-file-wrapper
        .d-flex.align-items-center
          = f.hidden_field :remove_audio, class: 'remove-file-checkbox'
          %a.remove-file-button.text-danger.mr-2.pointer
            %i.far.fa-trash-alt

          = link_to f.object.audio_url, target: "_blank", class: 'btn btn-link p-0 d-block text-truncate' do
            = f.object.audio_identifier
