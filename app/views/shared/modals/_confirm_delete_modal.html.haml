#confirmModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "exampleModalLabel", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %h5#exampleModalLabel.modal-title{'data-content': t('shared.confirm_delete', name: '_type_')}
          %i.fas.fa-exclamation-triangle.text-danger
          %span.message-title
          / Message title to replace

        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×

      .modal-body{'data-content': t('shared.are_you_sure_to_delete', type: "_type_", name: '_name_')}
        / Message body to replace

      .modal-footer
        %button.btn.btn-secondary{"data-dismiss" => "modal", :type => "button"}= t('shared.cancel')

        / Message url to replace
        = button_to t('shared.yes_delete'), '#', method: :delete, class: 'btn btn-danger'
