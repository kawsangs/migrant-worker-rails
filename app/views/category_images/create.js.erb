<% if @category_image.new_record? %>
  alert("Failed to upload painting: <%= j @category_image.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#new_category_image").before("<%= j render('category_image', category_image: @category_image) %>");
<% end %>
