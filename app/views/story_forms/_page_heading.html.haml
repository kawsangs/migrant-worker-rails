= content_for :title do
  = t("sidebar.your_story")

.d-flex.justify-content-end.mb-2
  = render "shared/buttons/add_new_button", url: new_story_form_path, name: t('sidebar.your_story')

  = render "shared/buttons/more_button" do
    %a.btn.dropdown-item{ href: story_forms_path(format: :json), target: '_blank' }
      %i.fas.fa-download.drop-menu-icon
      %span= t('shared.download_json')
