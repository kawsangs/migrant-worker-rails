.modal-body
  - @quiz.form.questions.each do |question|
    - if answer = @quiz.answers.select{|a| a.question_id == question.id}.first.presence
      %div.mb-3
        %div= question.name
        .d-flex{style: 'background-color: rgba(0, 0, 0, 0.05)'}
          %div
            %strong= answer.value
            - if answer.voice_url.present?
              %audio{controls: true}
                %source{src: answer.voice_url}
