= link_to 'new country', new_country_path

- @countries.each do |country|
  = country.name
  = link_to 'edit', edit_country_path(country)
  = link_to 'delete', country_path(country), data: { method: :delete, confirm: 'are you sure?' }